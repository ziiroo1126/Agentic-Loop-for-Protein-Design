# Git 提交与推送身份

- 本项目的 commit 作者与提交者统一使用 `ziiroo1126 <wangzr519@gmail.com>`。
- Push 统一使用 GitHub 账号 `ziiroo1126`，向本项目配置的远端推送。不要使用其他账号代替。
- 本机使用已验证的 `~/.ssh/id_ed25519_ziiroo1126` 专用密钥；仓库级 `core.sshCommand`
  和 `remote.origin.pushurl` 已配置。身份配置仅作用于本仓库，不修改全局 Git 配置。
- 后续执行 commit 和 push 时沿用此约定，无需再次询问身份；若命令环境覆盖 Git 身份，
  为本次命令显式指定上述作者、提交者和 SSH 身份。

# 项目范围与验收

- ALPD 是纯计算研究与软件项目，不开展湿实验。湿实验不属于项目路线图、发布条件或研究完成条件，不应将其缺失列为待补齐事项。
- 软件验收关注计算流程、输入与动作校验、可追溯记录、可复现运行及宿主和可视化体验；方法研究通过独立计算评测、等条件基线比较、消融与成本分析检验。
- 可以复用公开数据中已有的实验标签进行离线、回顾性评测，须注明来源并遵守测试信息隔离。计算评分与回顾性结果不等同于本项目新设计的实测结合或功能。

# 下载与代理流量

- 本服务器的订阅代理流量有限。执行文件下载、依赖安装、仓库克隆等数据传输时，优先复用本地缓存，并优先尝试直连；直连可用时，不使用订阅节点下载。
- 即使当前会话继承了代理环境变量，也应为下载命令及其子进程单独设置直连，并检查工具自身的代理配置。不要为此关闭 Clash 服务或修改 agent 自身的联网环境。
- 直连失败后，先区分网络不可达、鉴权失败和地址错误；确实需要代理时，再对该下载单独启用代理。不要仅因代理可能更快就自动切换。
- 分派下载任务给子 agent 时，一并传递这条约束。
